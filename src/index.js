import React from "react";
import ReactDOM from "react-dom";
import DataGrid from "./components/content/index";
const mountPoint = document.querySelector("#mountPoint");

ReactDOM.render(<DataGrid />, mountPoint);
